// =====================================================
// MINTORAIN Zellij 5분할 팀 에이전트 레이아웃
// Windows / macOS / Linux 모두 동일하게 동작
//
// 사용법:
//   Windows: pwsh mintorain-team.ps1
//   macOS/Linux: zellij --new-session-with-layout mintorain-zellij.kdl -s mintorain
//
// 패인 구성 (5개 모두 claude 자동 실행):
//   👑 Leader     (좌측, 메인 대화)
//   📋 Planner    (우측 1)
//   🎨 Frontend   (우측 2)
//   ⚙️  Backend   (우측 3)
//   🔍 QA         (우측 4)
//
// 사용 팁: 각 패인에 들어간 직후 첫 메시지로
//   "너는 기획자(또는 프론트엔드/백엔드/QA) 역할이야. CLAUDE.md / AGENTS.md 를 따라 답해줘."
// 라고 컨텍스트를 부여하면 역할별 응답이 명확해집니다.
//
// 패인 이동: Ctrl+P → 화살표  /  세션 종료: Ctrl+Q  /  재연결: zellij attach mintorain
// =====================================================

layout {
    default_tab_template {
        children
        pane size=2 borderless=true {
            plugin location="zellij:status-bar"
        }
    }

    tab name="mintorain-team" focus=true {
        pane split_direction="vertical" {
            // 좌측: 리더 (메인 대화 패인)
            pane size="55%" name="👑 Leader" focus=true {
                command "claude"
            }

            // 우측: 4개 전문 에이전트 패인 (모두 claude 자동 실행)
            pane split_direction="horizontal" {
                pane name="📋 Planner" {
                    command "claude"
                }
                pane name="🎨 Frontend" {
                    command "claude"
                }
                pane name="⚙️ Backend" {
                    command "claude"
                }
                pane name="🔍 QA" {
                    command "claude"
                }
            }
        }
    }
}
